import {
  CodeSurfer,
  CodeSurferColumns,
  Step,
} from "code-surfer";
import { github, vsDark } from "@code-surfer/themes";

export const theme = github;

# Remixing Constraint Validation

Edmund Hung

Remix Conf 2023

---

<CodeSurfer>

```html
<input name="email" />
```

```html 1[21:42]
<input name="email" type="email" required />
```

</CodeSurfer>

---

<CodeSurfer>

```jsx title="Login form example"
import { Form } from "@remix-run/react";

export default function LoginForm() {
  return (
    <Form method="post">
      <div>
        <label>Email</label>
        <input
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
      </div>
      <div>
        <label>Password</label>
        <input
          name="password"
          type="password"
          required
        />
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx title="Enhnacing form validation"
import { Form } from "@remix-run/react";

export default function LoginForm() {
  return (
    <Form
      method="post"
      onSubmit={(event) => {
        // Check validity of each field
        if (!event.currentTarget.reportValidity()) {
          // Prevent default form submission
          event.preventDefault();
        }
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
      </div>
      <div>
        <label>Password</label>
        <input
          name="password"
          type="password"
          required
        />
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx title="Capturing validation messages"
import { Form } from "@remix-run/react";
import { useState } from "react";

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          type="password"
          required
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx title="Capturing validation messages"
import { Form } from "@remix-run/react";
import { useState } from "react";

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        // Reset error
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        // Prevent default error bubble
        event.preventDefault();

        // Update message based on the input name
        setError((error) => ({
          ...error,
          [input.name]: input.validationMessage,
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          type="password"
          required
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx title="Customize messages"
import { Form } from "@remix-run/react";
import { useState } from "react";

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          type="password"
          required
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx title="Done"
import { Form } from "@remix-run/react";
import { useState } from "react";
import { login } from "~/auth.server";

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const value = Object.fromEntries(formData);

  return await login(value);
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          type="password"
          required
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```diff 17:22 title="Really?"
```

</CodeSurfer>

---

# Never trust the client

---

<CodeSurfer>

```jsx 49:69 title="Continue"
import { Form } from "@remix-run/react";
import { useState } from "react";
import { login } from "~/auth.server";

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const value = Object.fromEntries(formData);

  return await login(value);
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          type="email"
          required
          pattern="[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+"
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          type="password"
          required
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx 65,74 title="Extract schema"
import { Form } from "@remix-run/react";
import { useState } from "react";

const schema = {
    email: {
        type: "email",
        required: true,
        pattern: "[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+",
    },
    password: {
        type: "password",
        required: true,
    },
};

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const value = Object.fromEntries(formData);

  return await login(value);
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          {...schema.email}
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          {...schema.password}
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```diff 4:14 title="Extract schema"
```

```jsx 1 title="Secret sauce"
import { parse } from "@conform-to/validitystate"
import { Form } from "@remix-run/react";
import { useState } from "react";

const schema = {
    email: {
        type: "email",
        required: true,
        pattern: "[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+",
    },
    password: {
        type: "password",
        required: true,
    },
};

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const value = Object.fromEntries(formData);

  return await login(value);
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          {...schema.email}
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          {...schema.password}
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx 30:42
import { parse } from "@conform-to/validitystate"
import { json } from "@remix-run/node";
import { Form } from "@remix-run/react";
import { useState } from "react";

const schema = {
    email: {
        type: "email",
        required: true,
        pattern: "[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+",
    },
    password: {
        type: "password",
        required: true,
    },
};

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const submission = parse(formData, {
    schema,
    formatValidity,
  });

  if (submission.error) {
    return json(submission.error, { status: 400 })
  }

  return await login(submission.value);
}

export default function LoginForm() {
  const [error, setError] = useState({});

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          {...schema.email}
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          {...schema.password}
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

```jsx 44:52 title="Final touch"
import { parse } from "@conform-to/validitystate"
import { json } from "@remix-run/node";
import { Form, useActionData } from "@remix-run/react";
import { useState, useEffect } from "react";

const schema = {
    email: {
        type: "email",
        required: true,
        pattern: "[^@]+@[A-Za-z0-9]+.[A-Za-z0-9]+",
    },
    password: {
        type: "password",
        required: true,
    },
};

function formatValidity(validity) {
  if (validity.valueMissing) {
    return "The field is required";
  }

  if (validity.typeMismatch || validity.patternMismatch) {
    return "The value is invalid";
  }

  return "";
}

export async function action({ request }) {
  const formData = await request.formData();
  const submission = parse(formData, {
    schema,
    formatValidity,
  });

  if (submission.error) {
    return json(submission.error, { status: 400 })
  }

  return await login(submission.value);
}

export default function LoginForm() {
  const lastSubmission = useActionData();
  const [error, setError] = useState(lastSubmission?.error ?? {});

  useEffect(() => {
    if (lastSubmission) {
      setError(lastSubmission?.error ?? {});
    }
  }, [lastSubmission]);

  return (
    <Form
      method="post"
      onSubmit={(event) => {
        setError({});

        if (!event.currentTarget.reportValidity()) {
          event.preventDefault();
        }
      }}
      onInvalidCapture={(event) => {
        const input = event.target;

        event.preventDefault();

        setError((error) => ({
          ...error,
          [input.name]: formatValidity(input.validity),
        }));
      }}
      noValidate
    >
      <div>
        <label>Email</label>
        <input
          className={error.email ? "error" : ""}
          name="email"
          {...schema.email}
        />
        <div>{error.email}</div>
      </div>
      <div>
        <label>Password</label>
        <input
          className={error.password ? "error" : ""}
          name="password"
          {...schema.password}
        />
        <div>{error.password}</div>
      </div>
      <button>Login</button>
    </Form>
  );
}
```

</CodeSurfer>

---

Demo
